{
  "name": "search-now-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "json-server": "^1.0.0-beta.15"
  },
  "scripts": {
    "mock:server": "json-server --watch mock/db.json --port 3001"
  }
}