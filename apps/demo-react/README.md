# Demo React App

To run the React demo app:

```bash
cd apps/demo-react
npm run dev
```

The app runs on `http://localhost:5173/`.

Make sure the mock server is also running from the workspace root:

```bash
npm run mock:server
```
