# Demo Angular App

To run the Angular demo app:

```bash
cd apps/demo-angular
npm run start
```

The app runs on `http://localhost:4200/`.

Make sure the mock server is also running from the workspace root:

```bash
npm run mock:server
```
